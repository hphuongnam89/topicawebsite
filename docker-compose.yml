services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      NODE_ENV: production
      NEXT_PUBLIC_SITE_URL: https://topicauni.edu.vn
      WORDPRESS_API_URL: https://topicauni.edu.vn/wp-json/wp/v2
      CMS_REVALIDATE_SECONDS: 300
    restart: always
