# Deployment

Can be deployed on standard Node.js environments or Vercel.
Includes Dockerfile for containerized deployments.

Run:
```bash
docker build -t topica-edu .
docker run -p 3000:3000 topica-edu
```
